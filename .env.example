# Optional: cloud features (auth, profile sync, live sessions, admin analytics).
# When unset the app runs local-only.
# VITE_SUPABASE_URL=
# VITE_SUPABASE_ANON_KEY=

# Optional: Anthropic Claude key for the AI planner.
# VITE_ANTHROPIC_API_KEY=
